<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Vue Liste -->
        <record id="view_payroll_tree" model="ir.ui.view">
            <field name="name">silina.payroll.tree</field>
            <field name="model">silina.payroll</field>
            <field name="arch" type="xml">
                <list string="Fiches de Paie" decoration-info="state=='draft'" decoration-success="state=='paid'" decoration-muted="state=='cancelled'">
                    <field name="name"/>
                    <field name="employee_id"/>
                    <field name="department_id"/>
                    <field name="date"/>
                    <field name="period_start"/>
                    <field name="period_end"/>
                    <field name="gross_salary" sum="Total Brut"/>
                    <field name="total_deductions" sum="Total Déductions"/>
                    <field name="net_salary" sum="Total Net"/>
                    <field name="currency_id" column_invisible="1"/>
                    <field name="state" widget="badge" decoration-info="state=='draft'" decoration-success="state=='paid'" decoration-warning="state=='confirmed'" decoration-danger="state=='cancelled'"/>
                </list>
            </field>
        </record>

        <!-- Vue Formulaire -->
        <record id="view_payroll_form" model="ir.ui.view">
            <field name="name">silina.payroll.form</field>
            <field name="model">silina.payroll</field>
            <field name="arch" type="xml">
                <form string="Fiche de Paie">
                    <header>
                        <button name="action_confirm" string="Confirmer" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                        <button name="action_mark_paid" string="Marquer comme Payé" type="object" class="oe_highlight" invisible="state != 'confirmed'"/>
                        <button name="action_create_bill" string="Créer Facture Fournisseur" type="object" invisible="state != 'confirmed' or bill_id"/>
                        <button name="action_reset_to_draft" string="Remettre en Brouillon" type="object" invisible="state not in ['confirmed', 'cancelled']"/>
                        <button name="action_cancel" string="Annuler" type="object" invisible="state in ['paid', 'cancelled']"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,paid"/>
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="%(account.action_move_in_invoice_type)d"
                                    type="action"
                                    class="oe_stat_button"
                                    icon="fa-file-text-o"
                                    invisible="not bill_id"
                                    context="{'default_move_type': 'in_invoice'}">
                                <field name="bill_id" widget="statinfo" string="Facture"/>
                            </button>
                        </div>

                        <div class="oe_title">
                            <h1>
                                <field name="name" readonly="1"/>
                            </h1>
                        </div>

                        <group>
                            <group>
                                <field name="employee_id" readonly="state != 'draft'"/>
                                <field name="department_id"/>
                                <field name="job_id"/>
                                <field name="date" readonly="state != 'draft'"/>
                            </group>
                            <group>
                                <field name="period_start" readonly="state != 'draft'"/>
                                <field name="period_end" readonly="state != 'draft'"/>
                                <field name="currency_id" invisible="1"/>
                            </group>
                        </group>

                        <notebook>
                            <page string="Éléments de Salaire" name="salary_elements">
                                <group>
                                    <group string="Revenus">
                                        <field name="basic_salary" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="allowances" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="overtime_amount" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="bonus" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="gross_salary" class="oe_subtotal_footer_separator" widget="monetary"/>
                                    </group>
                                    <group string="Déductions">
                                        <field name="social_security" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="tax" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="other_deductions" readonly="state not in ['draft', 'confirmed']"/>
                                        <field name="total_deductions" class="oe_subtotal_footer_separator" widget="monetary"/>
                                    </group>
                                </group>
                                <group>
                                    <group>
                                        <field name="net_salary" class="oe_subtotal_footer_separator" widget="monetary" style="font-size: 18px; font-weight: bold;"/>
                                    </group>
                                </group>
                            </page>

                            <page string="Paiement" name="payment">
                                <group>
                                    <group>
                                        <field name="payment_method" readonly="state == 'paid'"/>
                                        <field name="payment_reference" readonly="state == 'paid'"/>
                                    </group>
                                    <group>
                                        <field name="payment_date" readonly="state != 'paid'"/>
                                    </group>
                                </group>
                                <group>
                                    <field name="notes" placeholder="Notes additionnelles..." readonly="state == 'paid'"/>
                                </group>
                            </page>

                            <page string="Communication" name="chatter">
                                <div class="oe_chatter">
                                    <field name="message_follower_ids" options="{'open_attachments': False}"/>
                                    <field name="activity_ids"/>
                                    <field name="message_ids" options="{'open_attachments': False}"/>
                                </div>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Vue Recherche -->
        <record id="view_payroll_search" model="ir.ui.view">
            <field name="name">silina.payroll.search</field>
            <field name="model">silina.payroll</field>
            <field name="arch" type="xml">
                <search string="Rechercher Fiches de Paie">
                    <field name="name"/>
                    <field name="employee_id"/>
                    <field name="department_id"/>
                    <separator/>
                    <filter string="Brouillon" name="draft" domain="[('state', '=', 'draft')]"/>
                    <filter string="Confirmé" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                    <filter string="Payé" name="paid" domain="[('state', '=', 'paid')]"/>
                    <filter string="Annulé" name="cancelled" domain="[('state', '=', 'cancelled')]"/>
                    <separator/>
                    <filter string="Ce mois" name="this_month"
                            domain="[('date', '&gt;=', (context_today() - relativedelta(day=1)).strftime('%Y-%m-%d')),
                                     ('date', '&lt;=', (context_today() + relativedelta(day=31)).strftime('%Y-%m-%d'))]"/>
                    <separator/>
                    <group expand="0" string="Grouper par">
                        <filter string="Employé" name="group_employee" context="{'group_by': 'employee_id'}"/>
                        <filter string="Département" name="group_department" context="{'group_by': 'department_id'}"/>
                        <filter string="État" name="group_state" context="{'group_by': 'state'}"/>
                        <filter string="Mois" name="group_month" context="{'group_by': 'date:month'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Vue Pivot -->
        <record id="view_payroll_pivot" model="ir.ui.view">
            <field name="name">silina.payroll.pivot</field>
            <field name="model">silina.payroll</field>
            <field name="arch" type="xml">
                <pivot string="Analyse des Salaires">
                    <field name="employee_id" type="row"/>
                    <field name="date" interval="month" type="col"/>
                    <field name="net_salary" type="measure"/>
                </pivot>
            </field>
        </record>

        <!-- Vue Graphique -->
        <record id="view_payroll_graph" model="ir.ui.view">
            <field name="name">silina.payroll.graph</field>
            <field name="model">silina.payroll</field>
            <field name="arch" type="xml">
                <graph string="Statistiques des Salaires" type="bar">
                    <field name="employee_id"/>
                    <field name="net_salary" type="measure"/>
                </graph>
            </field>
        </record>

        <!-- Action -->
        <record id="action_payroll" model="ir.actions.act_window">
            <field name="name">Fiches de Paie</field>
            <field name="res_model">silina.payroll</field>
            <field name="view_mode">list,form,pivot,graph</field>
            <field name="context">{'search_default_this_month': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Créer une nouvelle fiche de paie
                </p>
                <p>
                    Gérez les salaires de vos employés en créant des fiches de paie mensuelles.
                </p>
            </field>
        </record>

    </data>
</odoo>
